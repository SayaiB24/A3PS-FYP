# Datasets and generated artifacts
data/
outputs/


# Model weights
yolov8s-seg.pt

# Virtual environments
.venv/
venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Secrets
.env

# OS / editor
.DS_Store
.vscode/
.idea/

# Phase IV extracted features (regenerate with scripts/extract_features.py)
data/features/

# Phase IV dashboard demo videos: raw.mp4 files copied in by
# scripts/build_dashboard_demo.py. They are ~23 MB each and fully derivable from
# data/nexar/videos, so only the small risk.json results are versioned. (The 64
# mp4s tracked before this rule stay tracked -- git does not retroactively
# untrack, and they keep the original demo clips working after a fresh clone.)
dashboard/clips/**/raw.mp4
